import 'package:flutter/material.dart';

class TicketsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tickets'),
      ),
      body: Center(
        child: Text('Tickets Content Here...'),
      ),
    );
  }
}