import 'package:flutter/material.dart';

class PoliceView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Police'),
      ),
      body: Center(
        child: Text('Police Content Here...'),
      ),
    );
  }
}